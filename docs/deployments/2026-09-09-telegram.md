# Telegram deployment — 2026-09-09

Deployed application revision: `325629403008187a2555b0eae5f24fb082d468e9`, from `codex/telegram-primary-research`. The stacked PRs remain unmerged. This revision includes the paper validation, X discovery, public project research, Telegram interface and persistent fill-receipt configuration changes.

## Server and recovery

- Existing Hetzner Ubuntu installation preserved. SSH access was restored through rescue mode by adding the dedicated public key; no rebuild or disk wipe.
- Existing Compose project `deploy`, checkout `/opt/rh-chain-trader`, PostgreSQL volume and bot identity `@Rhjuice_bot` retained.
- Root-only backup: `/root/rh-deploy-backups/20260909T233710Z`. Contains the prior environment, Compose file, git revision, a 4,568,661-byte custom-format database dump, old image references and saved paper-fill receipt IDs. Old API/collector/Telegram images have rollback tags recorded in `images.json`. The dump completed successfully; restoration has not been rehearsed.
- Additive migrations `0006_social_discovery.sql` and `0007_project_research.sql` applied transactionally to the existing database. Initial seed migrations were not rerun.
- API, collector and Telegram images built successfully and replaced; PostgreSQL was not recreated. API remains bound to localhost.
- New shared approval credential generated only on the server and supplied to API and Telegram, not the collector. Existing Telegram and RPC credentials preserved. No secrets committed or printed.
- `deploy_telegram_state` now stores research-alert signatures and paper-fill receipts. Three existing receipt IDs were restored before the new Telegram worker started.

## Verified

- API, collector and Telegram running with zero restarts; PostgreSQL healthy. API health reports database connected, and actual database-backed reads succeed.
- `/research/projects`, `/discovery/accounts`, `/positions` and `/purchase-proposals` return HTTP 200 against the existing database.
- Telegram `getMe` identifies the existing bot; no webhook conflicts with long polling. Worker logs show live long polling and an incoming `/balance` command, without Telegram errors during verification.
- Deployed `/help`, `/projects` and `/research @tradedotcv` command handlers render real database-backed cards. This handler check did not send extra messages or mutate project settings. A persisted research-alert signature shows that the first automatic report delivery completed successfully.
- Approve and reject endpoints both return 403 without the credential and for a Grok actor even with the credential. The authenticated Telegram owner reaches a nonexistent proposal and receives 404. No proposal was approved or rejected during deployment tests.
- Public research produced the Trade.cv report: homepage retrieved, linked `test.trade.cv` returned HTTP 200, certificate-transparency lookup unavailable. Coverage 5%; overall rating withheld and issuer/token/deployer legitimacy unverified.
- The collector resumed ingesting chain events. One startup `eth_getLogs` HTTP 429 occurred; subsequent ingestion continued. Launch polling cursor persistence remains an existing operational improvement to address separately.

## Current controls and remaining work

- `ENABLE_TRADING=false`, `ENABLE_TX_SUBMISSION=false`; no live signer or automatic entry/exit implementation.
- `PROJECT_RESEARCH_ENABLED=true`. Trade.cv was observed paused after startup; that setting was preserved. The owner can use `/resume @tradedotcv` in Telegram.
- `X_DISCOVERY_ENABLED=false`: TwitterAPI.io adapter, credentials and provider integration validation remain pending, as requested.
- `GROK_RESEARCH_ENABLED=false`: optional paid xAI enrichment was not activated. Grok research/draft interfaces remain available, with Telegram as the only approval channel.
- Subdomain presence is supporting evidence, not issuer verification. Copycat protection requires the still-missing official issuer/token/deployer verification pipeline before future live execution.
- Full research callback interaction and post-deployment restart/deduplication acceptance testing were not independently exercised. Durable state files were confirmed present, and existing receipts loaded successfully. No guarantee of exactly-once Telegram delivery is made.

Recovery should use the recorded previous revision, configuration and tagged images while preserving the existing PostgreSQL volume. Never run `down -v`. Review the additive schema changes before any database restore; do not overwrite post-deployment data automatically.
